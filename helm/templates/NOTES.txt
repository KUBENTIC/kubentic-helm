Kubentic Operator installed successfully in namespace: {{ include "kubentic-operator.namespace" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACCESS DASHBOARDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run the following to get external IPs (may take ~60s on first install):

  kubectl get svc -n {{ include "kubentic-operator.namespace" . }} \
    kubentic-grafana vlogs-server vmsingle-vmstack-additional-service

VictoriaLogs VMUI (pod logs):
  http://<vlogs-server EXTERNAL-IP>:9428/select/vmui/

VictoriaMetrics VMUI (cluster metrics):
  http://<vmsingle-vmstack-additional-service EXTERNAL-IP>:8428/vmui/

{{- if index .Values "victoria-metrics-k8s-stack" "grafana" "enabled" }}

Grafana:
  http://<kubentic-grafana EXTERNAL-IP>  (admin / {{ index .Values "victoria-metrics-k8s-stack" "grafana" "adminPassword" }})
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CHECK STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  kubectl get kubenticagent -n {{ include "kubentic-operator.namespace" . }}
  kubectl get pods -n {{ include "kubentic-operator.namespace" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  # Operator logs
  kubectl logs -n {{ include "kubentic-operator.namespace" . }} -l app.kubernetes.io/name=kubentic-operator --tail=50

  # Vector log-shipper status (sends pod logs to VictoriaLogs)
  kubectl logs -n {{ include "kubentic-operator.namespace" . }} -l app.kubernetes.io/name=kubentic-vector --tail=50

  # Agent job history
  kubectl get jobs -n {{ include "kubentic-operator.namespace" . }}
